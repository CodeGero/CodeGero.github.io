## 32 Developer Tools. One $9 License. Lifetime.

Stop juggling a drawer full of half-maintained utilities. One suite replaces them all — scaffold, audit, secure, test, and ship from CLI and CI.

---

### 🏗️ Project Scaffolding
Generate production-ready projects in 2 seconds. 8 templates — Python, Go, Rust, React, Node.js, full-stack, API, CLI.

```bash
pip install kryptorious-devflow
devflow init my-api --template api
```

### 🛡️ All-in-One Quality Gate
Lint, format, type-check, security scan, and test — one command or one GitHub Action.

```bash
uses: CodeGero/python-guardian@v1
```

### 🔒 Secret Scanner
Catch hardcoded API keys and tokens before they reach your repo.

```bash
uses: CodeGero/secret-scanner@v1
```

### 🚀 Release Automation
Auto-detect version bumps from commits, generate changelogs, create releases.

```bash
uses: CodeGero/release-forge@v1
```

### 🤖 ML Code Audit
Catch missing seeds, deprecated APIs, data leakage, and pickle exploits.

```bash
uses: CodeGero/ml-guardian@v1
```

### 📊 CSV Analyzer
Detect encoding issues, type mismatches, duplicates, and score file health.

```bash
pip install kryptorious-csvclean
```

### 📦 Metadata Hygiene
Catch version drift, stale changelogs, missing license files, and leaked secrets before you publish.

```bash
pip install kryptorious-metaguard
```

### 🧪 Test Generator
Auto-generate pytest tests from Python source.

```bash
pip install kryptorious-testforge
```

### 📚 All 32 Tools Included

**PyPI (12):** DevFlow, GitSweep, DataForge, EnvGuard, TestForge, CSVClean, JSONGuard, APIGuard, DraftGuard, MetaGuard, wfGuard

**GitHub Actions (15):** Python Guardian, Release Forge, PR Sentinel, Secret Scanner, Stale Bot, Docker Guardian, Spell Guardian, Merge Guardian, ML Guardian, Dependency Bot, AI Code Lint, Coverage Gate, Go Guardian, Rust Guardian, License Guardian, Dependency Auditor

**npm (5):** DevKit, EnvGuard, LogGuard, ConfigSync, PortCheck

**DevFlow Premium (included with this license):** multi-environment CI/CD configs (staging auto / production with manual approval gate), Dockerfile + docker-compose, and per-environment Terraform stubs. Activate with `devflow activate <KEY>`.

---

### What You Get

✅ All 32 tools — core features, MIT licensed
✅ DevFlow Premium: multi-env pipeline generator, approval gates, infra-as-code templates
✅ Unlimited personal use
✅ Unlimited commercial use
✅ Lifetime access, all 1.x updates included

---

### Requirements

- Python 3.9+ (for PyPI tools)
- Node.js 18+ (for npm tools)
- Git (for DevFlow and GitSweep)

### License

Single-user, lifetime. Core tools MIT licensed. DevFlow Premium included.
